ui <- fluidPage(
  titlePanel("Analysis of the Relationship Between Life Expectancy and Race"),
  tabsetPanel(
    type = "tabs",
    tabPanel("Question 1", 
             sidebarLayout(
               sidebarPanel(
                 
               ),
               mainPanel(
               )
             ) 
    ),
    tabPanel("Question 2",
             sidebarLayout(
               sidebarPanel(
                 
               ),
               mainPanel(
               )
             )
    ),
    tabPanel("Question 3",
             sidebarLayout(
               sidebarPanel(
                 
               ),
               mainPanel(
               )
             )
    ),
    tabPanel("Question 4",
             sidebarLayout(
               sidebarPanel(
                 
               ),
               mainPanel(
               )
             )
    )
    
    
    
  )

)
